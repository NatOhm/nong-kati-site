// Re-export — 17-folder.md §9 locates the shared hook here; the context/provider
// implementation itself lives in src/providers/ThemeProvider.tsx (04-design-system.md §11.3).
export { useTheme, type Theme } from '@/providers/ThemeProvider';
